// https://aka.ms/vscode-remote/devcontainer.json
// https://github.com/microsoft/vscode-dev-containers/tree/master/containers/docker-existing-dockerfile
{
  "name": "Polynomial Masking DevContainer",
  "dockerFile": "Containerfile",
  "features": {
    "ghcr.io/devcontainers/features/git:1": {
      "version": "latest",
      "ppa": "false"
    }
  },
  "containerEnv": {
    "VIRTUAL_ENV": "/workspaces/discounted-poly-masking/.venv",
    "PATH": "/workspaces/discounted-poly-masking/.venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
  },
  "postCreateCommand": "python3 -m venv /workspaces/discounted-poly-masking/.venv --system-site-packages && /workspaces/discounted-poly-masking/.venv/bin/pip install -r requirements.txt",
  "postStartCommand": "sh -lc 'lsusb -d 2b3e: | while read -r _ bus _ dev _; do dev=${dev%:}; sudo chmod a+rw \"/dev/bus/usb/$bus/$dev\"; done || true'",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode.makefile-tools",
        "ms-azuretools.vscode-docker",
        "ms-vscode.cpptools-extension-pack",
        "ms-python.python",
        "ms-toolsai.jupyter"
      ]
    }
  },
  "runArgs": [
    "--privileged",
    "-v",
    "/dev/bus/usb:/dev/bus/usb"
  ],
  "remoteUser": "vscode"
}